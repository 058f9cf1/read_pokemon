#ifndef TABLES_H
#define TABLES_H

const char* status_condition(int);
const char* type(int);
const char* gen_2_item(int);
const char* catch_rate_to_item(int);
const char* move_name(int);
const char* species(int);
const char gender_list(int, int);
const char pk1_character_map(int);
const int pk1_pk2_species(int);

#endif
